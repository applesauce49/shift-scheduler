# Backend environment variables (copy to backend/.env)

# Core
NODE_ENV=development
MONGODB_URI=mongodb://localhost:27017/shift-scheduler
SESSION_SECRET=change-me

# Google Calendar API (Service Account)
# Calendar ID is typically an email like xxx@group.calendar.google.com or the primary account email
GOOGLE_CALENDAR_ID=c_c25abd047450745d1b10d89505f03b09f84caabd293dcbb5f45a241aa59d91d8@group.calendar.google.com

# Service account client email from the JSON key
GOOGLE_CLIENT_EMAIL=scheduler-cal-import@virtualoplm.iam.gserviceaccount.com

# Private key from the JSON key. Keep the literal \n characters between lines or quote and escape them.
GOOGLE_PRIVATE_KEY=

# Timezone used when querying calendar (IANA tz like UTC, America/New_York, Asia/Jerusalem)
GOOGLE_TIMEZONE=America/New_York

